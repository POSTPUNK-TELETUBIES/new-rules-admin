import { ProposalAction } from '../../src/types/proposal'
import { RuleLanguage, RuleSeverity, RuleType } from '../../src/types/ruleEnums'

export const ruleSeverityValues = Object.values(RuleSeverity)
export const ruleTypeValues = Object.values(RuleType)
export const ruleLanguageValues = Object.values(RuleLanguage)

export const proposalActionValues = Object.values(ProposalAction)
