import { Skeleton } from '@mui/material'

const SectionCuracionSkeleton = () => {
  return <Skeleton animation='wave' variant='rounded' height={200} />
}

export default SectionCuracionSkeleton
